package web

/*
import (
	"net/http"
	"../tools"
	"../config/utils"
	"github.com/julienschmidt/httprouter"
)

func sulfuHandler(fn func(http.ResponseWriter, *http.Request, httprouter.Params, map[int]map[int]tools.Bid, *utils.API), ps httprouter.Params, bids map[int]map[int]tools.Bid, api_c *utils.API) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		fn(w, r, ps, bids, api_c)
	}
}



func httproute*/
